# Odoo
alias up11='docker-compose --file /home/fabio/projects/code137/odoo/nds/docker-compose.yml up -d'
alias up12='docker-compose --file /home/fabio/projects/code137/odoo/odoo12/docker-compose.yml up -d'
alias up13='docker-compose --file /home/fabio/projects/code137/odoo/odoo13/docker-compose.yml up -d'
alias up14='docker-compose --file /home/fabio/projects/code137/odoo/odoo14/docker-compose.yml up -d'
alias down11='docker-compose --file /home/fabio/projects/code137/odoo/nds/docker-compose.yml down'
alias down12='docker-compose --file /home/fabio/projects/code137/odoo/odoo12/docker-compose.yml down'
alias down13='docker-compose --file /home/fabio/projects/code137/odoo/odoo13/docker-compose.yml down'
alias down14='docker-compose --file /home/fabio/projects/code137/odoo/odoo14/docker-compose.yml down'
alias odooshell11='cd /home/fabio/projects/odoo11/odoo && ./odoo-bin shell -c odoo-config -d'
alias odooshell12='odoo12 && cd /home/fabio/projects/odoo12/odoo && ./odoo-bin shell -c odoo-config -d'
alias co11='git checkout 11.0'
alias pull11='cd /mnt/e/Desenvolvimento/python/odoo11/trustcode-addons && co11 && git pull && cd ../odoo-brasil && co11 && git pull && cd ../trustcode-enterprise && co11 && git pull && cd ../odoo && co11 && git pull && cd ../python-boleto && git checkout master3 && git pull && cd ../PyTrustNFe && git checkout master3 && git pull && cd ../odoo-reports && co11 && git pull'
alias coverage11='cd /home/fabio/projects/odoo11/odoo && coverage run ./odoo-bin -c odoo-config --test-enable --log-level='test' --xmlrpc-port=8050 -d teste_unitario_11 --stop-after-init --init='

# Venvs
alias pyenv='source .venv/bin/activate'

# Postgres
alias postgres='sudo service postgresql start'
alias pgadmin4='~/venvs/pgadmin4/pgadmin4.sh'

# Ubuntu
alias rm='sudo rm'
alias apt='sudo apt'
alias service='sudo service'
alias kill='sudo kill -9'
alias killall='sudo killall -9'
alias cp='rsync --progress'
alias backup='rsync -arvz --delete'
alias cl='clear'
alias gpg='gpg2'
alias calc='qalc'

# Vim 
#alias vim='vim.gtk'
#alias vims='vim -S'
if [[ $(which nvim) ]]; then
  alias vim='nvim'
fi

# Dotfiles
alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'

# Docker
alias docker-clean=' \
  docker container prune -f ; \
  docker image prune -f ; \
  docker network prune -f ; \
  docker volume prune -f '
alias up='docker-compose up -d'
alias down='docker-compose down'

# WSL
alias explorer='explorer.exe'

# Esa
alias ls='/home/fabio/.asdf/installs/rust/1.42.0/bin/exa -G  --color auto --icons -a -s type'
alias ll='/home/fabio/.asdf/installs/rust/1.42.0/bin/exa -l --color always --icons -a -s type'

# Flutter
alias flu='powershell.exe flutter'

# Taskwarrior-tui
alias tt='taskwarrior-tui'
